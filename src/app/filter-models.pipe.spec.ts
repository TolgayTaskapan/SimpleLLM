import { FilterModelsPipe } from './filter-models.pipe';

describe('FilterModelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModelsPipe();
    expect(pipe).toBeTruthy();
  });
});
